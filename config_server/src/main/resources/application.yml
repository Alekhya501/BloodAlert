server:
  port: 8888h
spring:
  application:
    name: config_server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./config-repo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
